# AGENTS.md

See [`CLAUDE.md`](CLAUDE.md) for the guide for AI coding agents working in this repo
(commands, module layout, public API, conventions, gotchas). That file is canonical;
this one exists because some tools look for `AGENTS.md` rather than `CLAUDE.md`.
